{
  "name": "pin-op-chrome",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:deps": "pnpm --filter @pin-op/protocol build && pnpm --filter @pin-op/browser-extension-core build",
    "build": "pnpm run build:deps && tsc --noEmit -p tsconfig.json && node ./esbuild.mjs",
    "package": "pnpm run build && web-ext build --overwrite-dest --artifacts-dir ../../artifacts --filename pin-op-chrome-0.3.0.zip --ignore-files package.json pnpm-lock.yaml tsconfig.json esbuild.mjs src test \"src/**\" \"test/**\" && node ../../tools/normalize-browser-archive.mjs ../../artifacts/pin-op-chrome-0.3.0.zip",
    "test": "pnpm run build:deps && vitest run",
    "typecheck": "pnpm run build:deps && tsc --noEmit -p tsconfig.json",
    "lint": "pnpm run build:deps && tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@pin-op/browser-extension-core": "workspace:*",
    "@pin-op/protocol": "workspace:*",
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@types/webextension-polyfill": "^0.12.5",
    "esbuild": "^0.21.5",
    "vitest": "^2.1.0"
  }
}
