{
  "name": "@pinop/browser-extension-core",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm --filter @pinop/protocol build && vitest run && pnpm run build && node test/public-export.mjs",
    "test:public": "pnpm --filter @pinop/protocol build && pnpm run build && node test/public-export.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json && pnpm run test:types:check",
    "test:types:check": "tsc --noEmit -p test/types.tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@pinop/protocol": "workspace:*",
    "lucide": "0.468.0",
    "zod": "^3.25.76"
  }
}
